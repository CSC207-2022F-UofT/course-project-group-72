/*
package screen;

public class MakeReportScreen implements View{
    User current_active_user;
    Review current_selected_review;

    public MakeReportScreen(User current_active_user, Review current_selected_review){
        this.current_active_user = current_active_user;
        this.current_selected_review = current_selected_review;
    }

    @Override
    public MakeViewRequestModel create(){
        ReportScreen report_screen = new ReportScreen(controller, current_selected_review, current_active_user);

        return new MakeViewRequestModel(report_screen, true);
    }
}
*/
